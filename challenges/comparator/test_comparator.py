import pytest
from comparator import Player


# once again, I wanted to test, but don't have a way to test with the way hacker rank has set this up :(

def test_exists():
    assert Player

def test_base_short():
    a = pass
    b = pass
    expected = comparator(a, b)
    actual = pass